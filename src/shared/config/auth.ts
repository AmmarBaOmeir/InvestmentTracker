/**
 * Client-side auth credentials (frontend only).
 *
 * Default login:
 *   username: admin
 *   password: admin123
 *
 * Change these values to update the login gate for local development.
 */
export const AUTH_CREDENTIALS = {
  username: "admin",
  password: "admin123",
} as const;
